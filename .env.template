# Dreamlake Python SDK Environment Variables

# Remote Server Configuration (optional - only needed for remote mode testing)
# If not set, tests will run in local mode only
DREAMLAKE_SERVER_URL=http://localhost:3000

# Authentication (optional - for remote mode)
# You can use either username (which auto-generates API key) or provide API key directly
DREAMLAKE_TEST_USER=test-user
# DREAMLAKE_API_KEY=your-jwt-token-here

# Local Testing Configuration
# Default local path for test data (will use temp directories if not set)
# DREAMLAKE_LOCAL_PATH=./test_data
